export interface DashboardStats {
  todayOrders: number;
  todayRevenue: number;
  activeStalls: number;
  onlineDrivers: number;
  deliveryIssues: number;
  ordersTrend: number[];
  revenueTrend: number[];
}
